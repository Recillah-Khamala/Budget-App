<nav class="navbar navbar-expand-md navbar-dark bg-primary px-3" style="background-color: #316caf !important">
  <% if controller_name != 'categories' %>
    <a class="navbar-brand" href="<%= categories_path %>">
      <i class="bi bi-arrow-left"></i>7
    </a>
  <% end %>
  <h1 class="navbar-brand mb-0 mx-auto text-white text-uppercase">
    <% if controller_name == 'expenses' %>
      <%= 'Expenses' %>
    <% else %>
      <%= 'Categories' %>
    <% end %>
  </h1>
  <button class="navbar-toggler ml-auto" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse flex-grow-0" id="navbarNav">
    <ul class="navbar-nav ml-auto">
      <li class="nav-item">
        <%= button_to 'Log Out', destroy_user_session_path, method: :delete, data: {turbo: false}, class: 'btn btn-outline-light' %>
      </li>
    </ul>
  </div>
</nav>